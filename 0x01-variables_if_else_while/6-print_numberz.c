#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints digit using putchar and int type
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int digit_n = 0;

	while (digit_n <= 9)
	{
		/*convert into ASCII code*/
		putchar(48 + n);
		digit_n++;
	}
	putchar('\n');

	return (0);
}
